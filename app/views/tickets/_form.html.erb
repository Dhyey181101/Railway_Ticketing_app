<%= form_with(model: ticket) do |form| %>
  <% if ticket.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
        <% ticket.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div>
    <%= form.label :username, style: "display: block" ,placeholder: "Username:" %>
    <%= form.label :username, value: current_user.username %>
  </div>
  <br>
  <div>
    <%= form.label :train_number, style: "display: block", placeholder:"Train Number" %>
    <%= form.label :train_number, value: @train.train_number %>
  </div>
  <br>

  

  <!--  <div>-->
  <%#= form.label :confirmation_number, style: "display: block" %>
  <%#= form.number_field :confirmation_number %>
  <!--  </div>-->

  <div class="field">
    <%= form.hidden_field :train_id, value: @train.id %>
  </div>

  <div>
    <%= form.submit "Confirm" %>
  </div>
<% end %>